from pinger import ping, scrape
from api import generate_list
# use api.py, this just for tests

if __name__ == '__main__':
    generate_list()
    # for x in scrape(True):
    #     print(x)
    # print(ping())
    pass
